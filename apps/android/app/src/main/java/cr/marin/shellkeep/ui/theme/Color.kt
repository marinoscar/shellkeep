package cr.marin.shellkeep.ui.theme

import androidx.compose.ui.graphics.Color

// Light scheme — mirrors the web app's default MUI primary/secondary tones.
val LightPrimary = Color(0xFF1F6FEB)
val LightOnPrimary = Color(0xFFFFFFFF)
val LightSecondary = Color(0xFF6E59A5)
val LightBackground = Color(0xFFF7F8FA)
val LightSurface = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF111418)

// Dark scheme — terminal-friendly, near-black surface.
val DarkPrimary = Color(0xFF8AB4F8)
val DarkOnPrimary = Color(0xFF0B1220)
val DarkSecondary = Color(0xFFB39DDB)
val DarkBackground = Color(0xFF0B0D10)
val DarkSurface = Color(0xFF12161B)
val DarkOnSurface = Color(0xFFE6E8EB)

// Terminal foreground/background — used inside the terminal pane regardless of app theme.
val TerminalBackground = Color(0xFF000000)
val TerminalForeground = Color(0xFFE6E6E6)
