# shellkeep
Browser-based terminal workspace manager that uses tmux to keep shell sessions alive across disconnects and devices. It lets users save server profiles, launch persistent terminal sessions, and resume the same workspace later from anywhere through a clean web interface.
